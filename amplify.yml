version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend
        - npm ci
        - npm install tailwindcss postcss autoprefixer --save-dev
    build:
      commands:
        - npm run build  # Entfernen Sie "cd frontend" hier
  artifacts:
    baseDirectory: frontend/.next
    files:
      - '**/*'
  cache:
    paths:
      - frontend/node_modules/**/*
      